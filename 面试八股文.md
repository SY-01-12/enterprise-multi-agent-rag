# 面试八股文 · 计算机基础概念篇

---

## 网络协议

---

**Q1：什么是 HTTP 协议？它的工作流程是怎样的？**

HTTP（超文本传输协议）是浏览器和服务器之间通信的规则。
工作流程：客户端（通常是浏览器）向服务器发送一个 HTTP 请求，包含请求方法（GET/POST 等）、目标 URL、请求头和可选的请求体。服务器收到后处理请求，返回一个 HTTP 响应，包含状态码（200 成功、404 未找到等）、响应头和响应体。
HTTP 是无状态协议——服务器不会记住上一次请求是谁发的，每次请求都是独立的。

---

**Q1-附：什么是请求头和请求体？如何区分它们？**

一个 HTTP 请求报文由三部分组成：起始行（请求方法 + URL + 协议版本）、请求头（Headers）、请求体（Body）。它们之间用空行分隔。

请求头（Headers）是**关于请求的元数据**——"我是谁、我要什么格式、我能接受什么"。
常见的有：`Content-Type`（告诉服务器 Body 里装的是什么格式，JSON 还是表单）、
`Authorization`（携带登录凭证 Token）、
`Accept`（告诉服务器我能接收什么格式的响应）、
`User-Agent`（告诉服务器我是什么浏览器/客户端）。

请求体（Body）是**请求实际要传输的数据**——注册时的用户名密码、发布文章时的标题正文。GET 请求没有 Body（只取数据不带东西），POST/PUT 请求通常有 Body。

类比：寄快递。Body 是包裹里面装的东西（数据本身），Headers 是贴在包裹外面的快递单（收件人、寄件人、物品说明）。快递员不需要打开包裹，看快递单就知道怎么处理。

---

**Q2：HTTP 和 HTTPS 有什么区别？**

HTTPS 就是 HTTP 外面包了一层 TLS/SSL 加密层。
核心区别有三个：一是加密——HTTP 明文传输，中间人抓到包就能看到内容，HTTPS 加密后抓到了也是乱码；二是身份验证——HTTPS 需要服务器提供 CA 机构颁发的证书，证明"我就是我"，防止钓鱼网站冒充；三是数据完整性——HTTPS 会校验数据在传输过程中有没有被篡改。
默认端口也不同，HTTP 用 80，HTTPS 用 443。

---

**Q3：常见的 HTTP 请求方法有哪些？分别对应什么操作？**

GET 用于获取资源（查）；POST 用于创建资源（增）；PUT 用于整体替换资源（改）；PATCH 用于部分修改资源（局部改）；DELETE 用于删除资源（删）。
还有一个 HEAD，和 GET 一样但不返回响应体，只返回响应头，常用于检查资源是否存在或获取文件大小。
OPTIONS 用于查询服务器支持哪些请求方法，这是 CORS 预检请求的触发点。

面试官可能追问：POST 和 PUT 的区别？
POST 是"提交数据给服务器处理，服务器决定资源标识"，多次调用会产生多个资源（非幂等）；PUT 是"把数据放到指定的 URL 位置"，多次调用结果一样（幂等）。

---

**Q4：常见的 HTTP 状态码有哪些？怎么分类？**

1xx 信息：请求已收到，继续处理。
2xx 成功：200 OK（请求成功），201 Created（创建成功，常用于 POST 后返回）。
3xx 重定向：301 永久重定向（搜索引擎会更新收录链接），302 临时重定向。
4xx 客户端错误：400 Bad Request（请求格式有问题），401 Unauthorized（未认证，没登录），403 Forbidden（已认证但没权限），404 Not Found（资源不存在），422 Unprocessable Entity（参数校验失败）。
5xx 服务端错误：500 Internal Server Error（服务器内部错误），502 Bad Gateway（网关/反向代理收到的上游响应无效），503 Service Unavailable（服务器暂时不可用，可能过载或维护中）。

---

**Q5：TCP 和 UDP 有什么区别？分别适用于什么场景？**

TCP 是面向连接的可靠协议——三次握手建立连接，有确认重传机制保证数据不丢、不乱序，传输完毕四次挥手断开连接。适用于对数据完整性要求高的场景：网页浏览、文件下载、邮件、数据库连接。
UDP 是无连接的非可靠协议——只管发不管到不到，没有握手和确认机制，但速度快、延迟低。适用于对实时性要求高但丢几个包无所谓场景：视频通话、直播、在线游戏、DNS 查询。

记忆口诀：TCP 是"我先打个电话确认你在线，然后逐条读给你听，读完确认你听懂了"；UDP 是"我拿大喇叭喊，听没听到是你的事"。

---

**Q5-附：TCP 三次握手和四次挥手是怎样的？为什么是三次而不是两次？**

三次握手（建立连接）：
第一步，客户端发 SYN 包——"在吗？我想连你。"客户端进入 SYN_SENT 状态。
第二步，服务器回 SYN + ACK 包——"在，收到你的请求了，我也同意连接。"服务器进入 SYN_RCVD 状态。
第三步，客户端回 ACK 包——"好，那我开始发数据了。"双方进入 ESTABLISHED 状态，开始传数据。

为什么不是两次握手？
如果只有两次，客户端发 SYN，服务器回 SYN + ACK 就算连接建立了。
但问题是：如果客户端发的第一个 SYN 包因为网络拥堵在路上卡了很长时间，客户端以为丢了就重发了一个 SYN，两端正常通信结束后关闭连接。
这时候之前卡住的旧 SYN 包终于到了服务器，服务器以为客户端又要连接，回了 SYN + ACK，但客户端根本不知道这事。
如果是三次握手，客户端收到这个意外的 SYN + ACK 不会回 ACK，服务器收不到第三步的确认就不会建立无效连接，避免了资源浪费。

四次挥手（断开连接）：

第一步，客户端发 FIN 包——"我说完了，准备挂电话。
"第二步，服务器回 ACK——"知道了，但我可能还有话没说完，你等我一下。
"第三步，服务器发 FIN 包——"我也说完了。
"第四步，客户端回 ACK——"好的，那我挂了。"客户端等 2 个 MSL（最长报文生存时间，约 2 分钟）后彻底关闭。
为什么客户端最后要等 2MSL？因为最后这个 ACK 可能丢——如果服务器没收到 ACK，会重发 FIN，客户端在 2MSL 内能收到重发的 FIN 并重新确认。

口诀：三握——"你好，在吗？/ 在 / 知道了"；四挥——"我说完了 / 知道了 / 我也说完了 / 好的（等两分钟再挂）"。

---

**Q6：什么是 DNS？输入一个网址到页面显示，经历了什么？**

DNS（域名系统）把域名（如 `www.baidu.com`）翻译成 IP 地址（如 `110.242.68.66`），因为机器之间通信走的是 IP 地址。
浏览器输入网址后的完整流程：
第一步，浏览器查自身缓存有没有这个域名的 IP；
第二步，查操作系统缓存（hosts 文件）；
第三步，向本地 DNS 服务器发起查询（递归查询）；
第四步，本地 DNS 服务器从根域名服务器开始逐级查询（迭代查询）：根域名服务器 → .com 顶级域服务器 → baidu.com 权威服务器 → 返回 IP。拿到 IP 后，浏览器通过 TCP 三次握手与服务器建立连接，如果是 HTTPS 还要进行 TLS 握手协商加密密钥，最后发送 HTTP 请求获取数据。

---

**Q7：Cookie、Session、Token 三种认证方式的区别是什么？**

Cookie 是浏览器端的存储机制，可以存 Session ID 或 Token，每次请求自动附带。
Session 是**服务端存储**的会话数据，客户端只拿一个 Session ID（通常存在 Cookie 里），每次请求服务器根据 ID 查出用户信息——服务器知道"谁是谁"。
Token（通常是 JWT）是**客户端存储**的一串加密字符串，用户信息直接编码在 Token 里，服务器不需要查任何存储，只要验证签名就能知道"谁是谁"——服务器不记，Token 本身说明一切。

---

**Q7-附：什么是 JWT？它的结构是怎样的？和 Session 比优缺点是什么？**

JWT（JSON Web Token）是一个自包含的认证令牌，由三部分组成，用 `.` 分隔：Header（头部）、Payload（载荷）、Signature（签名）。

Header 声明了算法类型，通常是 `{"alg": "HS256", "typ": "JWT"}`。
Payload 是实际数据，包含注册声明（标准字段）：`sub`（主题，通常存用户 ID）、`exp`（过期时间）、`iat`（签发时间），也可以放自定义字段如 `username`。
Signature 是签名，把 Header + Payload 用 Base64 编码后，加上密钥用 HS256 算法算出——`HMACSHA256(base64(Header) + "." + base64(Payload), secret)`。
任何人拿到 Token 都能解码看 Header 和 Payload（Base64 不是加密，直接能解码），但拿不到密钥的人无法伪造签名。

JWT 相比 Session 的优势：无状态——不需要在服务器端存储会话，每台服务器独立验证 Token，天然支持水平扩展和微服务架构；跨域友好——Token 放在 HTTP Header 中传输，不受 Cookie 跨域限制；性能更好——省去了每次请求查数据库或 Redis 获取会话信息的开销。

JWT 的劣势：无法主动撤销——Token 签发后无法"踢人下线"，只能等过期或者引入黑名单（额外存储）；体积大——相比 Session ID 的一小串字符，JWT 通常有几百字节，每次请求都要传输；Payload 明文可见——不能放敏感信息（如密码），只能用 HTTPS 保护传输过程。

使用建议：access token 设置短过期时间（15-30 分钟），配合 refresh token（更长有效期，存在服务端）实现自动续期。Token 存在客户端的 localStorage 或 httpOnly Cookie 中，每次请求放在 `Authorization: Bearer <token>` 请求头中发送。

---

**Q8：什么是跨域？为什么会有同源策略？**

浏览器的同源策略规定：JavaScript 只能访问和当前页面同源（协议 + 域名 + 端口完全相同）的资源。不同源即为跨域。
这个策略是为了安全——假设你登录了银行网站，同时打开了恶意网站，如果没有同源策略，恶意网站的 JavaScript 就能向银行网站发请求（携带你的 Cookie），以你的身份转账。
虽然同源策略不能阻挡服务端发起的跨域请求（curl、Postman 不受影响），但它保护了浏览器端的普通用户。跨域的解决方案：服务端在响应头加 `Access-Control-Allow-Origin` 字段（即 CORS，跨域资源共享），或者用 JSONP（只支持 GET）、反向代理等。

---

## RESTful API

---

**Q9：什么是 RESTful API？设计原则是什么？**

REST（表述性状态转移）是一套 API 设计风格，不是协议。
核心原则：一是资源导向——URL 中只出现名词（资源名），不出现动词。比如 `/articles/` 而不是 `/getArticles/`。
二是用 HTTP 方法来表达操作——GET 获取、POST 创建、PUT 修改、DELETE 删除，同一个 URL 配合不同方法完成不同操作。
三是无状态——服务器不保存客户端状态，每个请求必须包含所有必要信息（Token 等）。
四是统一接口——返回格式一致（通常是 JSON）、分页/过滤/排序用查询参数标准化。

---

**Q10：RESTful URL 设计有哪些常见规范？**

URL 用复数名词（`/articles/` 而不是 `/article/`），层级关系体现为路径嵌套（`/articles/{id}/comments/`），过滤和搜索用查询参数（`?keyword=xxx&category_id=2`），
分页用 `page` 和 `page_size`（`?page=1&page_size=10`）。不要出现动词（❌ `/createArticle`），动作由 HTTP 方法表达。URL 中带版本号 `/api/v1/` 为将来升级留空间。

---

## 数据库

---

**Q11：什么是数据库索引？为什么要建索引？什么时候不该建索引？**

索引就像书的目录，让你快速找到某章某节的位置而不需要从第一页翻到最后。
不加索引，数据库做全表扫描，数据多了就慢。加了索引，数据库用 B+Tree （平衡多路搜索树）快速定位，查询耗时从 O(n) 降到 O(log n)。
不该建索引的情况：字段的值很少变化但重复率很高（如性别，只有男女，区分度极低）；频繁增删改的表（每次写都要更新索引，拖慢写入）；数据量很小的表（全表扫描比走索引还快，因为索引本身也有 I/O 开销）。

---

**Q12：什么是事务（Transaction）？ACID 分别是什么意思？**

事务是把多个数据库操作打包成一个不可分割的整体，要么全部成功，要么全部回滚。典型场景是转账：A 账户扣 100 + B 账户加 100 必须是原子操作，不能扣了钱没加上。

ACID 是事务的四个特性：
A（Atomicity 原子性）——事务中的操作要么全做要么全不做；
C（Consistency 一致性）——事务前后数据库都处于合法状态（不会出现余额为负等违反约束的情况）；
I（Isolation 隔离性）——并发的事务之间互不干扰，各自看到的数据要么是事务前要么是事务后，不会看到中间态；
D（Durability 持久性）——事务一旦提交，数据就永久保存了，就算数据库崩溃重启也不会丢。

---

**Q13：Redis 是什么？和 MySQL 有什么区别？**

Redis（Remote Dictionary Server）是一个基于内存的键值存储数据库。它的核心定位不是"替代 MySQL 做持久化存储"，而是"在 MySQL 前面做高性能缓存"。
MySQL 是关系型数据库，数据存在磁盘上，支持 SQL 复杂查询（JOIN、子查询、聚合函数）、事务 ACID，适合做持久化存储。
Redis 数据存在内存中，读写极快（微秒级），支持多种数据结构（字符串、哈希、列表、集合、有序集合），但内存容量有限，重启可能丢数据（除非开启持久化）。

两者不是竞争关系而是互补关系。典型搭配：读请求先查 Redis，命中直接返回（快）；未命中则查 MySQL，查到后写入 Redis 并设置过期时间。写操作先更新 MySQL（保证持久化），再删除 Redis 对应缓存（保证一致性）。

---

**Q13-附1：Redis 为什么这么快？**

四个核心原因，缺一不可。

第一，纯内存操作。MySQL 每次查询要走磁盘 I/O（机械硬盘寻道 10ms 级别，SSD 约 0.1ms），Redis 数据全在内存中，访问延迟在微秒级，两者差 3-4 个数量级。

第二，单线程模型。Redis 核心操作（读写、过期删除、数据淘汰）是单线程事件循环，没有多线程的锁竞争、上下文切换开销。注意：Redis 6.0 引入多线程只是处理网络 I/O 的读写（收发数据包），核心数据操作仍是单线程，保证原子性。

第三，高效数据结构。Redis 内部对每种数据结构做了极致优化。比如 List 底层用双向链表或压缩列表（ziplist），Hash 小数据时用压缩列表、大数据转哈希表，ZSet 用跳表（skiplist）+ 哈希表组合实现。
这些结构都是为内存访问模式专门设计的，不是照搬关系型数据库的磁盘 B+Tree。

第四，I/O 多路复用。一个线程同时监听多个 TCP 连接，哪个有数据到了就处理哪个，不会傻等某个慢客户端。

---

**Q13-附2：Redis 的典型使用场景有哪些？**

最常见五个场景。缓存：把 MySQL 的热点数据缓存到 Redis，请求先查 Redis，命中直接返回，未命中查 DB 回写缓存，设置合理的过期时间。这是 Redis 最核心的应用场景。

分布式 Session：多台应用服务器共享同一个 Redis，用户登录后 Session ID 对应的 Session 数据存到 Redis 中，任何一台服务器都能取出，实现无状态的水平扩展。

分布式锁：用 `SETNX key value NX EX 10`（原子操作：若 key 不存在则设置，且设 10 秒过期），多台服务器抢同一个 key 来争锁。释放时用 Lua 脚本原子校验 value（防误删别人的锁），实现互斥访问共享资源。

计数器/排行榜：点赞数、阅读量等高频更新的计数用 `INCR` 命令原子递增。排行榜用 ZSet（有序集合），score 存分数（阅读量/点赞数），member 存文章 ID，`ZREVRANGE` 取 Top N。

消息队列：List 的 `BLPOP`/`BRPOP` 做阻塞消费，Stream（Redis 5.0+）支持消费者组、ACK 确认机制，可做轻量级消息队列。不过 Redis 队列不持久（可能丢消息），重要消息场景还是选 RabbitMQ/Kafka。

---

**Q13-附3：Redis Key 的命名规范是什么？**

不是语法约束，而是行业约定，目的是防止键名冲突、方便运维排查。

第一，用冒号 `:` 做层级分隔，体现业务层级关系。比如 `toutiao:article:view_count:15` 一看就知道是头条项目下文章模块中 id=15 的浏览量。
第二，前缀统一项目名或模块名，避免多个应用共用一个 Redis 实例时 key 冲突。比如 `toutiao:session:user:4`。
第三，key 不要太长（占用内存）也不要太短（可读性差），5-30 个字符为宜。第四，key 中包含 ID 时使用统一命名风格（下划线还是驼峰），全项目保持一致。

---

**Q13-附4：Redis 数据的过期机制是怎样的？**

Redis 使用"惰性删除 + 定期删除"双策略来清理过期键。

惰性删除：客户端访问某个 key 时，Redis 先检查它是否过期，过期了就删掉并返回空。优点是没有额外 CPU 开销，缺点是如果一个 key 过期后从未被访问，它就一直占着内存（内存泄漏风险）。

定期删除：Redis 每 100ms 执行一次后台任务，随机抽一批设置了过期时间的 key，检查和删除其中过期的。不会一次扫描全部 key（阻塞单线程），而是分批随机抽查。如果一批中过期比例超过 25%，继续抽查下一批，直到过期比例低于阈值。

两者结合，正常访问频率的 key 靠惰性删除及时清理，冷数据靠定期删除兜底。
如果内存还是满了，走内存淘汰策略——Redis 的 `maxmemory-policy` 配置：`volatile-lru`（在设了过期时间的 key 中淘汰最近最少用的）、`allkeys-lru`（在所有 key 中淘汰 LRU）、`noeviction`（满了直接拒绝写入，默认策略）等。

---

**Q14：SQL 注入是什么？怎么防范？**

SQL 注入是攻击者通过用户输入拼接恶意 SQL 语句来操控数据库。
比如登录框输入用户名 `' OR 1=1 --`，拼出的 SQL 变成 `SELECT * FROM user WHERE username='' OR 1=1 --' AND password='...'`，`OR 1=1` 永远为真，`--` 注释掉了后面的条件，攻击者无需密码就能登录。
防范方式：第一条也是最重要的一条——**永远不要拼接 SQL 字符串**，使用参数化查询（Prepared Statement）。
ORM 框架（如 SQLAlchemy）内部就是用参数化查询，传参时用户输入始终被当作"值"而不是 SQL 代码的一部分，从根本上杜绝注入。
第二道防线是最小权限原则——应用连接数据库的账号只给必要的权限（增删改查），不给 DROP TABLE、创建用户等高危权限。

---

## 并发与操作系统

---

**Q15：进程、线程、协程的区别是什么？**

进程是操作系统分配资源的最小单位，每个进程有独立的内存空间，进程间通信需要借助管道、共享内存等机制。
线程是 CPU 调度的最小单位，同一个进程内的线程共享该进程的内存空间，通信方便但要注意线程安全问题。
协程是用户态调度的"轻量级线程"，在同一个线程内切换，不需要操作系统介入，切换成本远低于线程。Python 的 `asyncio` 就是协程模型。

面试官常追问：为什么协程比线程快？

因为线程切换需要在内核态和用户态之间来回切换（保存寄存器、切换栈等），单次切换耗时约几微秒；
协程切换完全在用户态完成（只切换函数调用栈），单次切换仅几十纳秒。而且协程避免了锁竞争——同一时刻只有一个协程在运行，不需要加锁保护共享数据。

---

**Q16：Python 的 GIL（全局解释器锁）是什么？它影响什么？**

GIL 是 CPython 解释器的一把大锁，规定同一时刻只有一个线程在执行 Python 字节码。
这意味着 Python 多线程无法利用多核 CPU——即使用 4 个线程，同一时刻也只有 1 个线程在真正执行 Python 代码。
GIL 对 I/O 密集型应用影响不大（因为 I/O 等待时线程会释放 GIL），但对 CPU 密集型应用严重影响性能。
绕过 GIL 的方式：用多进程（`multiprocessing`）代替多线程，每个进程有独立的 GIL；或者把 CPU 密集型部分用 C 扩展实现，在 C 代码中可以主动释放 GIL；或者用 `asyncio` 的协程模型在单线程内通过异步 I/O 实现并发，规避 GIL 问题。

---

**Q17：同步、异步、阻塞、非阻塞这四个概念怎么理解？**

同步和异步描述的是"调用者"的行为：
同步是你打电话，一直等对方接；异步是你发了条微信，不用等着，对方回了你再看。
阻塞和非阻塞描述的是"被调用者"的状态：阻塞是电话占线，你啥也干不了只能等；
非阻塞是你拨了号码立刻得到结果（通了或没通），你可以决定接下来做什么。四个组合中"异步 + 非阻塞"效率最高：发完请求去干别的事，底下完成时通知你回来处理——这正是 FastAPI 事件循环的工作方式。

---

## 版本控制

---

**Q18：Git 的基本工作流程是什么？常用命令有哪些？**

Git 的核心是三棵树：工作区（你电脑上的文件）、暂存区（`git add` 放进去的、准备提交的文件）、本地仓库（`git commit` 后正式保存的快照）。
日常流程：`git pull` 拉取最新代码 → 修改文件 → `git add .` 加到暂存区 → `git commit -m "消息"` 提交到本地仓库 → `git push` 推送到远程仓库。
`git branch` 查看/创建分支，`git checkout -b feat/xxx` 创建并切换到新分支，`git merge` 合并分支，`git stash` 暂存未提交的改动，`git log` 查看提交历史。

---

**Q19：`git merge` 和 `git rebase` 的区别是什么？**

merge 把两个分支的最新提交合并在一起，生成一个新的"合并提交"（merge commit），保留完整的分支历史，别人能看到你什么时候从主分支拉了分支、什么时候合并回去。
rebase 把你当前分支上的提交"搬"到目标分支的最新提交之后，变基后提交历史是一条直线，没有分叉，看起来清爽但丢失了分支信息。

黄金规则：**永远不要对已经推送到远程的公共分支做 rebase**，因为 rebase 会改写提交历史，别人基于你的旧提交开发时会产生混乱。个人开发分支可以 rebase，合并到 master/main 时用 merge。

---

## 容器化与部署

---

**Q20：什么是 Docker？它和虚拟机有什么区别？**

Docker 是一种容器化技术，把应用和它的依赖打成一个镜像，在任何机器上运行效果一致，解决"我这能跑你那不行"的问题。
Docker 和虚拟机的核心区别：虚拟机在物理机之上运行完整的操作系统，每个 VM 有独立的内核，启动慢（分钟级）、占用资源大；Docker 容器共享宿主机的操作系统内核，只隔离应用层，启动快（秒级）、资源开销小。
打比方：虚拟机是独栋别墅（独立地基独立管道），Docker 是公寓楼（共享地基和管道，每家隔开）。

---

**Q21：什么是 CI/CD？**

CI（持续集成）指开发者频繁把代码合并到主分支，每次合并自动触发构建和测试，尽早发现集成问题。
CD 有两种解读：持续交付（Continuous Delivery）——代码随时可以一键部署到生产环境但需要人工确认；持续部署（Continuous Deployment）——代码通过测试后自动部署到生产环境不需要人工介入。
CI/CD 的工具链：GitHub/GitLab 做代码托管，Jenkins/GitHub Actions/GitLab CI 做流水线编排，Docker 做应用的打包和运行环境标准化。

---

## 数据结构与算法

---

**Q22：数组和链表的区别是什么？什么时候用哪个？**

数组是连续内存空间，支持根据下标 O(1) 随机访问，但在中间插入/删除需要移动后面所有元素（O(n)）。
链表节点分散在内存中，每个节点指向下一个，随机访问必须从头遍历（O(n)），但插入/删除只要改指针指向（O(1)，前提是已知插入位置）。
用数组的场景：需要频繁按位置访问、数据量变化不大。用链表的场景：频繁在中间插入/删除、数据量经常增减。
数据库索引的 B+Tree 本质就是结合了两者优点——叶子节点之间用指针连接（像链表，支持范围查询），内部节点是排序的多叉树（像数组，支持快速定位）。

---

**Q23：什么是哈希表（Hash Table）？**

哈希表是一种根据键直接访问值的结构，通过哈希函数把键映射到数组下标。平均情况下增删查都是 O(1) 时间复杂度。
关键概念：哈希冲突——不同键算出了同一个下标，解决方法有拉链法（冲突的元素挂在同一个位置形成链表）和开放寻址法（往下找空位放）。
Python 的 dict 就是用哈希表实现的。
Redis 本质上就是一个超强的"分布式哈希表"。
数据库的哈希索引（如 MySQL Memory 引擎）也是一个哈希表，但哈希索引只支持等值查询（`WHERE id = 5`），不支持范围查询（`WHERE id > 5`），这是哈希表的天生缺陷——数据不按顺序存放。

---

## 设计模式

---

**Q24：什么是 MVC 模式？**

MVC（模型-视图-控制器）是将应用分为三层的架构模式。
Model（模型）管数据和业务逻辑——数据库操作、数据校验在这层。
View（视图）管展示——HTML 界面、JSON 响应。
Controller（控制器）管协调——接收请求、调 Model 拿数据、选 View 展示结果。三层各司其职，改界面不影响数据逻辑，改数据库不影响前端。
在 Web 项目中，数据库表定义是 Model，返回的 JSON 格式定义可看作 View，路由函数是 Controller。

---

**Q25：什么是单例模式（Singleton）？你在项目里见过吗？**

单例模式保证一个类全局只有一个实例。
在 Web 项目中，数据库连接引擎（Engine）本质就是单例——全局只有一个线程安全的连接池，所有请求共用，不会每次请求都新建连接。
Python 中的 `@lru_cache()` 也是一种单例实现方式——第一次调用时创建，之后的调用都返回同一个缓存对象。应用配置对象（Settings）也是单例，全局一份即可，不需要反复读取配置文件。

---

**Q26：什么是工厂模式？和直接 `Class()` 实例化有什么区别？**

工厂模式不直接 `new 一个对象`，而是通过一个"工厂函数/类"来创建对象，创建逻辑封装在工厂里。
好处是：调用方不需要知道具体创建了哪个子类，工厂可以根据条件返回不同类型。比如一个支付系统可能有支付宝支付、微信支付、银行卡支付，调用方只调 `PaymentFactory.create("alipay")`，工厂内部决定返回哪个具体支付类。
在 Python 中，工厂模式不像 Java 里那么重，一个简单的 `if/else` 或者字典映射就是工厂的雏形。

---

## 综合概念

---

**Q27：什么是 JSON？为什么 API 返回 JSON 而不是 XML？**

JSON（JavaScript 对象表示法）是一种轻量级的数据交换格式，语法极其简单——用花括号表示对象、方括号表示数组、键值对表示属性。
为什么不用 XML？XML 要写开闭标签（`<user><name>张三</name></user>`），JSON 只要 `{"name": "张三"}`，体积小传输快。
此外 JSON 是 JavaScript 的原生数据结构，前端直接 `data.name` 拿值；XML 需要解析 DOM，代码啰嗦。现在 JSON 是所有主流 API 的事实标准。

---

**Q28：什么是序列化和反序列化？**

序列化是把内存中的数据结构或对象转成可存储或可传输的格式（通常是字节串或字符串，如 JSON）。
反序列化就是把 JSON 字符串还原成内存中的对象。
举个例子：Python 中一个 `User` 对象有 `name` 和 `age` 属性，序列化成 JSON 就是 `{"name": "张三", "age": 25}` 发给前端；前端收到后反序列化回 JavaScript 对象，然后就能取 `data.name` 了。

---

**Q29：对称加密和非对称加密的区别是什么？**

对称加密只有一把钥匙，加解密用同一把，速度快适合大量数据加密，问题在于怎么安全地把钥匙给对方。
非对称加密有两把钥匙——公钥加密的只能用私钥解，私钥签名的任何人都能用公钥验证。HTTPS 的做法是混合两种：用非对称加密（RSA/ECDHE）先安全交换一把临时的对称密钥，之后通信都用这把对称密钥（AES）加密。
这样既保证了安全性（公钥公开不怕被截获），又保证了速度（大量数据用对称加密）。

---

## Elasticsearch

---

**Q30：什么是 Elasticsearch？它和 MySQL 有什么区别？**

Elasticsearch（简称 ES）是一个基于 Lucene 的分布式全文搜索引擎。
它和 MySQL 的核心区别在于定位不同：MySQL 是关系型数据库，擅长精确查询（`WHERE id = 5`）和事务 ACID 保证。
ES 是搜索引擎，擅长全文检索（"搜索包含'Python 教程'的文章"）和海量数据的聚合分析。
底层结构完全不同：MySQL 用 B+Tree 索引按行存储数据，ES 用倒排索引按词存储数据——把文档内容分词后，建立"词 → 文档列表"的映射，搜索时直接查这个词对应的文档列表，不需要逐行扫描。
两者不是替代关系而是互补关系：MySQL 做持久化存储和事务写入，ES 做搜索和分析，通过数据同步（Canal、Logstash）保持两边数据一致。

---

**Q30-附1：什么是倒排索引？它是怎么工作的？**

倒排索引是 ES 的核心数据结构，理解它就理解了 ES 为什么搜索这么快。
正排索引是"文档 → 词"的映射——"文档1 包含 Python、教程、入门"——MySQL 的全表扫描就是这种模式，查"Python"需要逐行读每篇文章的内容去匹配。
倒排索引反过来，是"词 → 文档列表"的映射。入库时 ES 把每篇文章内容分词，为每个词建立一个"倒排列表"，记录该词出现在哪些文档中："Python" → [文档1, 文档3, 文档7]；"教程" → [文档1, 文档2, 文档5]；"入门" → [文档1, 文档4, 文档6]。
当用户搜索"Python 教程"时，ES 分别查"Python"的倒排列表和"教程"的倒排列表，取交集就得到同时包含两个词的文档——[文档1]。整个搜索过程不需要扫描任何文档的原始内容，只操作词汇索引，所以极快。
倒排列表中不止记录文档 ID，还记录词频（TF，这个词在文档中出现了几次）、位置（在文档的第几个字符，用来支持高亮和短语匹配）、偏移量等信息，这就是 ES 能做相关性评分的底层数据基础。

---

**Q30-附2：ES 的核心概念有哪些？**

类比关系型数据库来理解 ES 的核心概念。
Index（索引）相当于 MySQL 的 Database 或 Table——一组文档的逻辑集合，有自己的 Mapping 定义和分片设置。创建索引时可以指定主分片数和副本分片数。
Document（文档）相当于 MySQL 的一行记录——ES 中最小的数据单元，是一个 JSON 对象。每个文档有唯一的 `_id`，可以自动生成也可以自己指定。
Field（字段）相当于 MySQL 的列——文档中一个个具体的键值对。
Mapping（映射）相当于 MySQL 的表结构定义——定义字段名、字段类型（text/keyword/long/date 等）、是否分词、使用什么分词器。ES 支持动态 Mapping——存入 JSON 时自动推断字段类型，但生产环境建议显式定义 Mapping 以避免类型误判。
Shard（分片）——ES 把索引的数据水平切分到多个分片，每个分片是一个独立的 Lucene 实例，分布式存储在不同节点上。分片数在索引创建时指定，创建后不可更改。
Replica（副本）——每个主分片可以有多个副本分片，副本有两个作用：一是数据冗余备份，二是分担查询负载。副本数在索引创建后可以动态调整。
Node（节点）——运行 ES 实例的一台服务器。多个节点组成 Cluster（集群），集群有一个唯一的名称，节点通过配置的集群名称自动发现并加入。

---

**Q30-附3：ES 中的 text 和 keyword 类型有什么区别？**

这是最常见的 Mapping 设计陷阱，搞混了会导致搜索行为和排序行为都不符合预期。
text 类型会被分词器拆分成一个个词条（term），用于全文搜索。比如"我爱北京天安门"存入 text 字段，经过 ik 分词器会变成 ["我", "爱", "北京", "天安门"]，搜"北京"能命中这篇文档。text 字段不支持直接聚合和排序（每个 field 会有很多 term，按哪个排序？），需要聚合时用其 `.keyword` 子字段。
keyword 类型不会被分词，整个字符串作为一个完整的词条存储。适合精确匹配和聚合的场景："张三"就是一个整体，搜"张"搜不到。典型使用场景：订单号、手机号、标签、枚举值、用户名。
实战中常用"多字段映射"：一个字段同时映射为 text 和 keyword 类型——`"name": {"type": "text", "fields": {"keyword": {"type": "keyword"}}}`。全文搜索用 `name`，精确匹配和聚合用 `name.keyword`。

---

**Q31：ES 的写入流程是怎样的？为什么是近实时搜索？**

ES 的写入不像 MySQL 那样立刻刷盘。一条数据写入 ES 的流程是：文档首先被写入内存中的索引缓冲区（Buffer），同时写入事务日志（Translog，预写日志）防止数据丢失。此时文档还不能被搜索到。默认每 1 秒 ES 执行一次 Refresh 操作——将内存缓冲区中的数据生成一个新的 Segment（段）并打开，Segment 可被搜索。这就是 ES 被称为"近实时搜索"（Near Real-Time，NRT）的原因——写入后最多 1 秒就可以被搜到，不是实时的。
但 Segment 还在内存中，如果服务器宕机内存数据会丢。ES 还有 Flush 操作——默认每 30 分钟或 Translog 达到 512MB 时，将内存中的 Segment 刷到磁盘（fsync），并清空 Translog。Flush 后才真正持久化。
简单的记忆顺序：写入 → 内存 Buffer + Translog → Refresh（1s，可搜索）→ Flush（30min，持久化到磁盘）。

---

**Q31-附：ES 的查询流程是怎样的？**

查询分为两阶段：Query Phase（查询阶段）和 Fetch Phase（获取阶段）。
Query Phase：协调节点把查询请求广播到索引的每个分片（主分片或副本分片），每个分片在本地执行查询，返回符合条件文档的 `_id` 和排序值（不会返回完整文档内容），协调节点汇总后对结果进行全局排序，取前 N 条。
Fetch Phase：协调节点根据取到的文档 ID 列表，向对应的分片请求完整文档数据，分片返回 `_source` 内容，协调节点最终组装成搜索结果返回给客户端。

---

**Q32：常见的 ES 查询类型有哪些？**

全文检索查询（针对 text 类型，会分词）——match：最常用，对搜索词分词后匹配，支持模糊程度配置。multi_match：在多个字段中同时搜索同一个关键词。match_phrase：短语匹配，搜索词作为一个整体按顺序匹配，词之间不能有其他词插入。
精确查询（针对 keyword 类型，不分词）——term：精确匹配单个词条，搜索词不经过分词器直接匹配，相当于 SQL 的 `=`。terms：匹配多个词条中的任意一个，相当于 SQL 的 `IN`。range：范围查询，`gte`/`lte` 控制起止范围，相当于 SQL 的 `BETWEEN`。
复合查询——bool：组合多个查询条件。must 所有条件都必须满足（相当于 AND），参与算分。filter 所有条件都必须满足（和 must 结果集相同），但不参与算分，ES 会自动缓存 filter 结果，性能比 must 好。should 至少满足一个条件（相当于 OR），参与算分。must_not 条件必须不满足（相当于 NOT），不参与算分，内部走 filter 上下文。

---

**Q33：ES 的分词器（Analyzer）是什么？IK 分词器有什么特点？**

分词器把一段文本拆分成一个个独立的词条（term），是全文搜索的基础。一个 Analyzer 由三部分组成：Character Filter（字符过滤器，预处理文本如去除 HTML 标签）→ Tokenizer（分词器，按规则切分文本）→ Token Filter（词条过滤器，对切分后的词条做处理如转小写、去停用词）。
ES 默认的 standard 分词器按空格和标点切分单词，适合英文但不适合中文——中文没有空格分隔，所以"我爱北京天安门"会被切成单个字"我"/"爱"/"北"/"京"/"天"/"安"/"门"，失去了词语的语义。
IK 分词器是中文分词的事实标准，提供两种模式：ik_max_word 做最细粒度切分——"我爱北京天安门"切成 ["我", "爱", "北京", "天安门"]，尽可能穷举所有可能的词，提高召回率但索引体积大。ik_smart 做粗粒度切分——"我爱北京天安门"切成 ["我", "爱", "北京", "天安门"]，简洁直接，适合大多数场景。
通常索引时用 ik_max_word（多切一些词，提升召回），搜索时用 ik_smart（精准分词，提升精度）。

---

**Q34：ES 的聚合（Aggregation）有哪几种类型？**

Bucket 聚合——分组，把文档分到不同的"桶"里。terms 聚合按字段值分组（相当于 SQL 的 `GROUP BY`），比如按文章分类统计数量。date_histogram 聚合按时间间隔分组，如按天/小时统计文章发布量。range 聚合按数值范围分组，如统计价格在 0-100、100-500、500+ 各区间商品数。
Metric 聚合——计算，对每个桶做数值计算。avg/max/min/sum 求平均/最大/最小/总和。stats 一次性返回 count、min、max、avg、sum 五个基本统计量。cardinality 去重计数（相当于 SQL 的 `COUNT(DISTINCT ...)`，有精度误差——ES 用 HyperLogLog 算法近似计算，适合大基数的近似去重场景）。
Pipeline 聚合——对聚合结果再做二次聚合。derivative 计算相邻桶的差值（求导数），如分析阅读量增长趋势。moving_avg 滑动窗口平均值，平滑波动曲线。

---

**Q35：ES 的性能优化有哪些常见手段？**

写入优化：批量写入用 Bulk API，一次推送多条文档，减少网络往返。写入高峰期临时调大 `refresh_interval` 到 30s 或 -1（关闭自动刷新），降低 Segment 合并频率，写完再改回来。使用 SSD 磁盘，I/O 是 ES 性能的物理上限。
查询优化：能用 filter 就不用 must——filter 结果会被 ES 自动缓存（Filter Cache），must 每次重新算分不缓存。分页不要用深层 `from + size`（如翻到 1000 页之后），每个分片需要把前 N 条数据全加载到协调节点再排序，性能急剧恶化。替代方案是 search_after（游标翻页，用上次结果的排序值定位下一页）或 scroll（用于全量导出，不适合实时分页）。
索引设计优化：合理设置分片数——一个分片大小建议在 10-50GB，过多分片导致协调节点合并开销大，过少分片导致单分片过大不利于分布式并行。不需要全文搜索的字段用 keyword 而非 text，keyword 的索引结构更紧凑。裁剪 Mapping——不搜索不聚合的字段设 `"index": false` 减少索引体积。

---

## LangChain 基础 · Prompt 工程

---

**Q36：什么是 LangChain？它的核心组件有哪些？**

LangChain 是一个用于构建大语言模型（LLM）应用的开发框架——注意它不是模型本身，而是一套工具和抽象，把 LLM 应用开发中的常见模式封装成标准组件。
核心组件包括：Model I/O 统一对接各种大模型的接口层，包含 LLM（文本补全模型）和 ChatModel（对话模型），通过统一接口屏蔽不同模型厂商的差异（OpenAI / Anthropic / 通义千问 / 文心一言等），切换模型几乎不需要改业务代码。
Prompt Template 提示词模板，把动态变量注入到预定义的 Prompt 结构中，支持模板复用和参数化管理。Chain 链，把多个组件串联成处理流水线。最简单的 Chain 是 "PromptTemplate + LLM"，复杂的 Chain 可以组合检索器、文档处理、输出解析等多个步骤。
Retrieval 检索增强生成（RAG）相关组件，包括 Document Loaders、Text Splitters、Embeddings、Vector Stores、Retrievers。Agent 智能体，让 LLM 自主决定调用哪个工具、按什么顺序调用，完成动态推理和任务执行。Memory 记忆，让对话具有上下文连续性，支持多种存储后端（内存、Redis、数据库等）。

---

**Q37：什么是 Prompt Template？项目中怎么用？**

Prompt Template 是把 Prompt 做成可复用的模板，模板中的变量在运行时动态注入。LangChain 提供了 `PromptTemplate`（用于 LLM，简单的字符串模板）和 `ChatPromptTemplate`（用于 Chat Model，支持多条 Message 组合）。
基本用法：定义一个模板字符串 `"请用{style}的风格写一篇关于{topic}的介绍"`，实例化 `PromptTemplate` 时传入模板和变量名列表，运行时传入 `style="幽默"` 和 `topic="机器学习"`，最终生成完整的 Prompt 发给 LLM。
ChatPromptTemplate 更常用——它可以组合多条 SystemMessage（设置 AI 的角色和行为）、HumanMessage（用户输入）、AIMessage（历史 AI 回复），实现结构化的对话管理。
SystemMessage 是 Prompt 工程的灵魂——通过 SystemMessage 可以设定 AI 的角色（"你是一个资深 Python 面试官"）、输出格式（"请用 JSON 格式回复"）、行为约束（"不要编造你不知道的内容"）。
实际项目中，建议把 Prompt 模板统一管理：放在单独的文件或常量模块中，不要在业务代码里硬编码 Prompt 字符串，方便调整迭代和团队协作。

---

**Q37-附：什么是 SystemMessage 和 HumanMessage？它们怎么搭配？**

这是 Chat Model 特有的消息类型概念。一次完整的对话由一系列 Message 组成，每个 Message 有固定的 role。
SystemMessage（系统消息）：给 AI 设定的全局角色、行为准则和输出规则，不会被用户看到，只用于指导模型行为。例如："你是头条新闻的智能编辑助手，擅长总结新闻要点。回答简洁，不超过 200 字。不要编造信息来源。"
HumanMessage（用户消息）：用户实际输入的问题或指令。
AIMessage（AI 回复消息）：模型之前的回复，把历史对话中的 AI 回复填入此角色，实现多轮对话的上下文连贯。
ToolMessage（工具消息）：Agent 调用工具后，工具返回的结果，LLM 根据该结果决定下一步推理。
一条完整的 Prompt 通常结构是：`[SystemMessage(角色定义+约束), HumanMessage(用户问题)]`。多轮对话时在后面追加 `AIMessage(历史回复) + HumanMessage(新问题)` 交替排列。SystemMessage 始终保持在最前面且只出现一次。

---

**Q38：什么是 Few-Shot Prompting？和 Zero-Shot 有什么区别？**

Zero-Shot Prompting 是不给任何示例，直接向模型提问——"这篇文章的情感是正面还是负面？"——模型凭预训练知识直接作答。
Few-Shot Prompting 在 Prompt 中提供几个示例（通常 2-5 个），让模型通过示例"学会"答题模式和格式，然后再问目标问题。比如先给两个示例"输入：这电影太烂了 → 情感：负面"和"输入：很满意这次购物 → 情感：正面"，然后问"输入：快递速度一般般 → 情感："，模型会根据示例的模式做出推理。
Few-Shot 的核心价值有两个：一是通过示例引导输出格式（让模型返回固定的结构化文本），二是提升推理准确率（模型从示例中"学会"了分类标准）。LangChain 提供了 `FewShotPromptTemplate`，自动管理示例选择和模板拼接。
几个实践要点：示例数量不是越多越好，3-5 个通常是性价比最高的；示例应覆盖不同类别和边缘情况；示例的质量（标注准确、格式一致）远重要于数量。

---

**Q38-附：什么是 CoT（Chain of Thought，思维链）？为什么要用？**

CoT（思维链）是在 Prompt 中引导模型"一步步推理"而不是直接给答案。核心做法是在 Prompt 或 Few-Shot 示例中加入推理步骤的描述。
不用 CoT："34 × 17 = ?" → 模型可能直接猜一个数，容易出错。
使用 CoT："34 × 17 = ? 让我们一步步算：34 × 10 = 340，34 × 7 = 238，340 + 238 = 578，所以答案是 578。" → 模型学会了推理过程。
CoT 为什么有效？LLM 是逐 token 预测下一个词——如果前几个 token 是推理过程，后续 token 就是在这个推理基础上生成的，自然更准确。直接跳到答案相当于跳过了所有中间推演，出错概率大增。而且 CoT 提供了可解释性——用户能看到模型是怎么得出结论的。
LangChain 中可以手动在 Prompt 中写 CoT 指令（"请一步步思考"），也可以在 Few-Shot 示例中展示完整的推理链。对于复杂推理任务（数学计算、逻辑推理、多步骤分析），CoT 是必选项。

---

**Q39：LangChain 的 Output Parser 是什么？常用类型有哪些？**

Output Parser 是把 LLM 的原始文本输出解析为结构化数据（如 JSON、Python 对象），解决 LLM 输出不可控的问题。LLM 返回的是自由文本，但下游代码需要的是确定的字段和类型——Output Parser 在这两者之间架起了桥梁。
StrOutputParser：最简单的解析器，直接返回模型输出的文本字符串。
JsonOutputParser：把输出解析为 JSON 对象。配合 Prompt 模板中的格式指令使用——Parser 会自动生成格式说明注入到 Prompt 中（`get_format_instructions()`），告诉模型应该输出什么结构的 JSON。
PydanticOutputParser：最推荐的结构化输出方案。定义一个 Pydantic 模型（如 `class ArticleSummary(BaseModel)` 包含 title、summary、tags 等字段），Parser 自动生成格式指令并解析输出为对应的 Pydantic 对象。好处是输出自带类型校验和 IDE 提示。
CommaSeparatedListOutputParser：把输出解析为逗号分隔的列表。
实战经验：Output Parser 不是万能的——LLM 偶尔还是会输出不合法内容（多了个逗号、字段名写错、多了一句解释的话）。建议在 Parser 外层加 try/except，解析失败时做重试或 fallback 处理。

---

**Q39-附：Runnable 接口和 LCEL 是什么关系？为什么现在都用 Runnable？**

首先要明确：LCEL 并没有被淘汰，它和 Runnable 是同一个东西的两个层面——Runnable 是底层协议接口，LCEL 是上层表达语法。LangChain 中一切组件（PromptTemplate、LLM、Retriever、Tool）都实现了 `Runnable` 接口，这个接口定义了统一的方法签名：`.invoke()`（同步调用）、`.ainvoke()`（异步调用）、`.stream()`（流式输出）、`.batch()`（批量处理）。只要一个对象实现了 Runnable 接口，就可以用管道符 `|` 串联——这就是 LCEL 的语法糖。

`chain = prompt | llm | output_parser` 的本质是创建了一个 `RunnableSequence`（Runnable 序列），数据从左到右流经每个 Runnable。你也可以用更显式的方式写——`RunnableParallel` 并行执行多个分支、`RunnablePassthrough` 透传数据不做处理、`RunnableLambda` 把任意函数包装成 Runnable、`RunnableBranch` 做条件路由。

用 Runnable 而非旧式 `LLMChain` 的原因：第一，所有组件遵循统一接口，切换和组合零成本。第二，自动获得异步（`.ainvoke()`）、流式（`.stream()`）、批处理（`.batch()`）能力，不需要额外配置。第三，中间步骤可访问和调试——每个 Runnable 节点的输入输出都可以 intercepted。第四，Runnable 是 LangGraph 的基石——LangGraph 中的 Node 本质就是 Runnable，学会 Runnable 才能平滑过渡到 LangGraph。

**补充：LangGraph 是什么？和 Runnable/LCEL 的关系？**

对于简单的顺序流水线（Prompt → LLM → Parser），LCEL/Runnable 足够。但当业务流程变复杂——需要条件分支（if/else）、循环（Agent 的 Thought-Action-Observation 循环）、人工审核节点（Human-in-the-Loop）、持久化状态——LCEL 的线性管道表达力就不够了。这时候需要 LangGraph。

LangGraph 用图（Graph）来编排 LLM 工作流：节点（Node）是 Runnable 单元（调 LLM、调工具、做判断），边（Edge）定义节点之间的流转规则（可以是条件边，根据 LLM 输出决定跳转到哪个节点），State（状态）在整个图中持久化传递和更新。Agent 的执行循环在 LangGraph 中就是一个环状图：agent 节点 → 判断是否需要调工具 → tool 节点 → agent 节点 → ... 直到判断可以结束。

LangGraph 本质上是对 Runnable 的图编排层——每个 Node 仍然是 Runnable，但编排逻辑从"管道串行"升级为"有向图"。这是目前 LangChain 生态构建复杂 Agent 和 Multi-Agent 系统的推荐方式。

---

## LangChain · RAG（检索增强生成）

---

**Q40：什么是 RAG？为什么需要 RAG 而不是直接问大模型？**

RAG（Retrieval-Augmented Generation，检索增强生成）是在 LLM 生成回答之前，先从外部知识库中检索相关文档，把检索到的文档和用户问题一起发给 LLM，让 LLM 基于检索到的上下文生成答案。
为什么需要 RAG？大模型有三大局限：第一，知识截止日期——训练数据有截至时间，不知道训练后发生的事。第二，幻觉——大模型会编造看起来合理但实际不存在的信息，尤其在专业领域。第三，私有知识盲区——大模型不知道公司内部文档、项目代码、私密业务规则。
RAG 解决了这三个问题：外部知识库可以随时更新（不用重新训练模型），LLM 基于检索到的真实文档生成回答（大幅减少幻觉），私有数据存到向量库中即可被检索（赋予模型私有知识）。
本质上是给 LLM 配了一个"随时查阅的资料库"——不用把所有知识记在脑子里（参数记忆），而是需要时去翻资料（检索），然后基于资料回答问题（生成）。

---

**Q40-附：RAG 的完整工作流程是怎样的？**

RAG 分为索引阶段（离线）和检索+生成阶段（在线）。
索引阶段（离线，文档入库时执行）：第一步，加载文档——从文件（PDF、Word、Markdown）、数据库、网页等数据源读取原始内容。第二步，文本分割——把长文档切成若干较短的文本块（Chunk），每个 Chunk 是检索的最小单位。第三步，向量化——用 Embedding 模型把每个 Chunk 转成向量（一组浮点数，如 1536 维），语义相近的文本向量距离更近。第四步，存储——把向量和原始文本一起存入向量数据库（如 Faiss、Chroma、Milvus、Pinecone）。
检索+生成阶段（在线，用户提问时执行）：第一步，用户问题向量化——用同一个 Embedding 模型把用户问题转成向量。第二步，相似度检索——在向量数据库中查找和问题向量最相似的 Top-K 个文本块（通过余弦相似度或欧氏距离）。第三步，拼接上下文——把检索到的文本块内容和用户问题一起组织成完整的 Prompt。第四步，生成回答——LLM 基于 Prompt 中的上下文生成最终答案。
LangChain 用 `RetrievalQA` 或 LCEL 表达式 `{"context": retriever, "question": RunnablePassthrough()} | prompt | llm | output_parser` 把整个流程串起来。

---

**Q41：文本分割（Text Splitting）有哪些策略？Chunk 大小怎么定？**

文本分割是 RAG 中最关键也最容易踩坑的环节。切得不好，检索效果就不好。
LangChain 内置的分割策略：RecursiveCharacterTextSplitter（最推荐）——按优先级依次尝试用段落换行、句子结束符、空格、单个字符来切分，确保优先保持语义段落的完整性，避免把一句话切成两半。CharacterTextSplitter——按单个分隔符简单切分（如按 `\n\n` 分段）。TokenTextSplitter——按 Token 数量切分（直接对应模型上下文窗口限制）。
Chunk 大小怎么定？Chunk 越大，每个 Chunk 包含的上下文越完整，但检索精度下降（搜到的一个大 Chunk 可能大部分内容不相关）。Chunk 越小，检索越精准，但可能丢失上下文导致 LLM 看不到完整的推理链。没有银弹，需要在具体场景中测试。
重叠（Overlap）是必须的——相邻 Chunk 之间应该有一段内容重叠（通常为 Chunk 大小的 10%-20%），这样即便边界刚好把关键信息切开了，相邻 Chunk 也能兜底。
实战经验：对于短文本问答（FAQ），Chunk_size 设为 512 tokens，overlap 设为 50。对于长文档问答（合同分析），Chunk_size 设为 1024-2048 tokens，overlap 设为 200。

---

**Q41-附：Embedding 是什么？常用 Embedding 模型有哪些？**

Embedding（向量嵌入）是把文本转成一串高维浮点数向量，使语义相近的文本在向量空间中距离更近。本质是将文本的语义信息"压缩编码"到一组数字中。
"我很开心" → [0.023, -0.451, 0.789, ..., -0.312]（1536 个数字）；"心情很好" → [0.018, -0.439, 0.795, ..., -0.301]（与上一句向量距离很近）；"今天下雨" → [-0.891, 0.234, -0.102, ..., 0.556]（与上一句向量距离很远）。向量距离越近 = 语义越相似，所以可以用向量做相似度检索。
常用 Embedding 模型：OpenAI 的 `text-embedding-ada-002`（1536 维，性能稳定，适合通用场景）和 `text-embedding-3-small`（更便宜更快）。国内可用：智谱的 `embedding-2`、BGE 系列（BAAI 出品，开源可本地部署，中文表现优秀）。本地部署场景常用 `sentence-transformers` 库加载 BGE 或 m3e 模型。
关键原则：索引时和查询时**必须用同一个 Embedding 模型**——不同模型生成的向量不在同一个语义空间中，向量维度不同或空间不对齐，相似度计算毫无意义。

---

**Q42：向量数据库（Vector Store）是什么？和传统数据库有什么区别？**

向量数据库是专门存储和检索高维向量的数据库。它和传统数据库的区别在于"查什么"：传统数据库按精确字段值查询（`WHERE name = '张三'`），向量数据库按向量相似度查询（"找和这个向量最相似的 K 个向量"）。
核心能力是 ANN（近似最近邻搜索）——在高维向量空间中快速找到最相似的 Top-K 个结果。之所以叫"近似"是因为高维空间中精确搜索代价极高（维度灾难），所以用近似算法（如 HNSW、IVF）在准确率和速度之间做权衡。
主流向量数据库：Chroma 轻量级，适合原型和小项目，Python 原生集成 LangChain。Faiss，Facebook 出品，性能极高但是纯向量搜索引擎不是数据库，数据在内存中，不支持持久化。Milvus 开源分布式向量数据库，支持十亿级别向量，适合生产环境。Pinecone 商业托管向量数据库，SaaS 服务，免运维。Weaviate 开源向量数据库，内置向量化和混合搜索。
LangChain 用统一的 `VectorStore` 接口封装了这些差异，切换向量库只需改一行初始化代码。

---

**Q42-附：Retriever 是什么？有哪些检索策略？**

Retriever（检索器）是 LangChain 中负责从外部知识源检索相关文档的组件。它和 VectorStore 的关系是：VectorStore 是存储层，Retriever 是查询接口层。通过 `vectorstore.as_retriever()` 即可把向量库包装成 Retriever。
基本检索策略：Similarity Search 纯向量相似度检索，最常用。MMR（最大边际相关性）在相似度的基础上增加多样性约束，避免搜出 K 条几乎相同的内容，牺牲一点匹配度换取结果多样性。
高级检索策略：MultiQueryRetriever 用 LLM 把用户原始问题改写成多个不同角度的问题分别检索，取并集去重，缓解单一查询表达不充分的问题。SelfQueryRetriever 让 LLM 从用户自然语言问题中提取语义过滤条件（如"去年发布的文章"→ `date >= 2025-01-01`），结合向量检索和元数据过滤。
ParentDocumentRetriever 先在小块 Chunk 上做精准检索（小块检索精度高），找到相关小块后返回其所属的大块父文档（父文档包含更完整上下文），兼顾检索精度和上下文完整性。这是生产环境最推荐的检索策略之一。
ContextualCompressionRetriever 检索后用压缩器（如 LLM 本身）过滤掉不相关的部分，只保留和问题真正相关的段落，减少噪声，提升 LLM 生成质量同时节省 Token。

---

**Q43：LangChain 的 Chain 类型（Stuff / Refine / Map-Reduce / Map-Rerank）有什么区别？**

这四种 Chain 类型的核心区别在于"如何把多篇检索到的文档和用户问题一起发给 LLM"，本质是应对文档过多超出模型上下文窗口的不同策略。
Stuff（填充法）：把所有检索到的文档全部拼接到一个 Prompt 里，一次性发给 LLM。最简单直接，所有上下文都在一次调用中。缺点是文档总长不能超过模型上下文窗口限制（如 4096 或 128K tokens），文档多了就放不下。适用场景：检索文档数量少、模型上下文窗口足够大时，这是首选方案。
Refine（精炼法）：迭代式处理。先把第一篇文档和问题发给 LLM，生成一个中间答案。再把第二篇文档和中间答案一起发给 LLM 改进答案。如此循环直到所有文档处理完毕。每次只处理一篇文档，不存在上下文溢出问题。缺点是串行调用 N 次 LLM，耗时长。且 LLM 可能在前几步跑偏，偏差逐轮累积。适用场景：文档不太多、对答案完善度要求高。
Map-Reduce（映射-归约法）：两阶段处理。Map 阶段并行地将每篇文档发给 LLM，各自总结出要点。Reduce 阶段把所有要点汇总发给 LLM，生成最终答案。Map 阶段天然并行，速度快。缺点是需要压缩每篇文档，压缩过程丢失细节。且消耗大量 Token（每篇文档各调用一次 LLM）。适用场景：文档很多、需要并行加速。
Map-Rerank（映射-重排序法）：Map 阶段让 LLM 对每篇文档打分（相关性评分），选出得分最高的文档直接作为答案来源。适用场景：答案不需要综合多篇文档，用最相关的单篇文章就能回答。

---

**Q43-附：RAG 的常见优化策略有哪些？**

RAG 效果不好往往不是某一个环节的问题，而是多种因素叠加。常见的优化方向：
检索前优化——问题改写：用户提的问题可能表意不清，在检索前用 LLM 把用户问题改写成更好的检索查询（Query Rewriting / HyDE）。HyDE（假设文档嵌入）的做法是先让 LLM 根据问题生成一篇假想的回答文档，再用这篇假想文档的向量去检索真实文档——因为 LLM 生成的文档和真实文档在表达风格上更接近，检索效果往往比直接拿简短问题去搜更好。
检索中优化——混合检索（Hybrid Search）：结合向量检索（语义相似）+ 关键词检索（BM25，精确词匹配），互补各自局限。向量检索擅长同义表达（"便宜"匹配到"价格低"），关键词检索擅长专有名词和精确匹配（产品型号、合同编号）。多路召回 + 重排序：同时用多种检索策略分别召回 Top-N，合并去重后用 Cross-Encoder（精准但慢的重排序模型）做二次筛选和精排，取最终 Top-K 进入生成阶段。
检索后优化：检索到的文档可能包含大量噪声（无关内容），可以在 Prompt 中显式要求 LLM"如果检索内容不相关，请说'不知道'而不是编造答案"。同时通过 ContextualCompressionRetriever 在进入 LLM 之前裁剪无关部分。
生成优化：Prompt 设计——明确指令"请只根据提供的内容回答，不要使用外部知识"。对于引用场景，要求 LLM 在回答中标注信息来源（"根据提供的第一段文档..."），提升可信度。

---

## LangChain · Agent（智能体）

---

**Q44：什么是 Agent？Agent 和 Chain 的区别是什么？**

Agent（智能体）是一个让 LLM 自主决策的执行单元。LLM 作为"大脑"分析用户意图，决定调用哪个工具、传什么参数、按什么顺序，循环执行直到得到最终答案。
Agent 和 Chain 的核心区别在于控制流：Chain 的执行路径是预先写死在代码中的固定顺序——先检索、再拼 Prompt、最后生成答案，每次路径完全一样。Agent 的执行路径是 LLM 在运行时动态决定的——根据当前状态和工具返回结果，LLM 自主决定下一步做什么、需要调什么工具、是否已经可以回答用户了。
类比：Chain 是"标准操作流程"——如同银行柜台，按固定步骤处理每个客户。Agent 是"智能助手"——给一个目标，它自己规划完成路径。
Agent 的核心三要素：LLM（推理引擎，决策者）、Tools（工具的集合，Agent 可调用的外部能力）、规划的编排逻辑（何时调用工具、何时停止）。LangChain 旧式写法用 `AgentExecutor` 实现这个循环，但现在更推荐用 LangGraph 构建 Agent——用图的环状结构（节点 + 条件边）来表达 Agent 的推理-行动循环，比 AgentExecutor 的 while 循环更灵活、更可控（支持 Human-in-the-Loop、断点恢复、子图嵌套等）。

---

**Q44-附：Agent 的执行循环（AgentExecutor）是怎样的？**

AgentExecutor 维持一个循环，直到任务完成或达到终止条件。每一步执行以下逻辑：
第一步，构造 Prompt——把 System Message（角色定义 + 可用工具列表描述）、历史对话消息、用户最新输入组合成完整的 Prompt 发给 LLM。
第二步，LLM 推理——LLM 分析当前状态，决定下一步行动。可能的输出：Action（需要调用工具，指明工具名和参数）或 Final Answer（已经可以回答用户了）。
第三步，执行判断——如果是 Final Answer，循环结束，将答案返回给用户。如果是 Action，调用对应的工具函数，得到工具返回结果（Observation）。
第四步，反馈——工具返回结果以 ToolMessage 的形式追加到对话历史中，回到第一步重新调用 LLM。LLM 根据工具返回的结果继续推理，直到得出 Final Answer。
关键设计：Agent 的执行不是无限制的——通常设置 `max_iterations`（最大循环次数，如 15 次）防止死循环或无限调用工具。Token 消耗也是重要考量——每次循环都要把完整对话历史发给 LLM，对话越长成本越高。

---

**Q45：常见的 Agent 类型有哪些？分别适用什么场景？**

OpenAI Functions Agent（最推荐）：基于 OpenAI 的 Function Calling 能力，LLM 直接输出结构化的函数调用 JSON（工具名 + 参数），由 AgentExecutor 解析后调用工具。响应最稳定，幻觉最少。需配合 OpenAI 或兼容 Function Calling 的模型使用。
ReAct Agent（经典）：基于 ReAct 论文的模式——Reasoning（推理）+ Acting（执行）。LLM 按固定格式输出：`Thought:（我的思考...）→ Action:（调工具）→ Action Input:（参数）→ Observation:（工具返回结果）→ Thought:（根据结果进一步思考...）→ 重复或 Final Answer:（最终答案）`。通用于任何模型，不依赖 Function Calling，但输出格式偶尔不标准需要容错处理。
Structured Chat Agent：支持多参数工具调用的 Agent，工具参数可以是复杂结构，适用于工具需要多个参数的场景。
Tool Calling Agent：LangChain 新推荐的统一接口，底层适配各模型的工具调用能力（OpenAI Function Calling / Anthropic Tool Use 等），是对各模型原生 Tool Calling 的统一抽象。
选择原则：模型支持 Function Calling 则优先 OpenAI Functions Agent 或 Tool Calling Agent。模型不支持则用 ReAct Agent。需要多参数工具则用 Structured Chat Agent 或 Tool Calling Agent。

---

**Q45-附：什么是 ReAct 模式？为什么它比纯推理或纯行动更好？**

ReAct = Reasoning + Acting，是让 LLM 交替进行推理和行动的模式。
纯推理（Reasoning Only / CoT）：模型在"脑海中"推理，但无法查外部信息，推理完全依赖训练数据的记忆。一旦需要实时数据或私有知识，推理就断了。
纯行动（Acting Only）：模型不断调工具但缺少推理规划，容易盲目调用——不知道调哪个、调到了之后怎么利用结果。
ReAct 融合了两种的优势：每一步先推理（Thought，我需要知道什么？接下来该调什么工具？），再行动（Action，调工具拿数据），拿到结果后再推理（Thought，这个结果说明了什么？还需要更多信息吗？还是已经够回答用户了？）。这种"推理→行动→观察→再推理"的循环，让模型在信息不完整时可以主动获取信息，在推理过程中有事实基础。
举例：用户问"今天北京和上海的温差是多少？"纯推理：模型瞎猜一个数（幻觉）。纯行动：模型可能一下查北京天气、一下查上海天气，然后把原始数据扔给用户，没有计算。ReAct：Thought（需要查北京和上海今天的气温）→ Action（查北京天气，得到 28°C）→ Action（查上海天气，得到 25°C）→ Thought（温差 = 28 - 25 = 3°C）→ Final Answer（今天北京比上海高 3 度）。

---

**Q46：Agent 中的 Tool 是什么？如何定义和注册一个 Tool？**

Tool 是 Agent 的能力扩展，本质就是一个函数 + 描述信息（名称、描述、参数 schema）。Agent 不是凭直觉决定调用什么函数的——它通过读取每个 Tool 的名称和描述来理解工具能做什么，然后决定应该使用哪个。
一个 Tool 的核心组成：name（工具名，Agent 用它来引用工具，如 "weather_search"）。description（工具的功能描述——"查询指定城市的实时天气"，这是 Agent 决定是否用的关键依据，描述必须清晰）。func（实际执行的函数）。args_schema（参数的 JSON Schema 定义，告诉 LLM 这个工具需要什么参数以及参数类型）。
LangChain 中定义 Tool 的常用方式：用 `@tool` 装饰器——最简单，直接在函数上加上装饰器，LangChain 自动从函数签名和 docstring 抽取名称、描述、参数 schema。或者用 `StructuredTool.from_function()` 显式指定所有字段，更精确。或者用 `Tool` 类手动指定。
Tool 的描述（description）是 Agent 靠谱与否的关键：描述写得不清晰，Agent 就会在该用的时候不用、不该用的时候乱用。每注册一个工具后都测试一次——Agent 是否能正确识别使用场景。

---

**Q46-附：什么是 Toolkit？常用的 Toolkit 有哪些？**

Toolkit 是一组相关 Tool 的集合包，Agent 获得这个包就获得了某个领域的所有能力。比如 SQL Toolkit 包含查表结构、执行查询、列出表名等工具。GitHub Toolkit 包含查 Issue、创建 PR、查文件等工具。
常用的内置 Toolkit：Python REPL Tool 让 Agent 可以执行 Python 代码做计算、数据处理。RequestsToolkit 发送 HTTP 请求，让 Agent 能调用外部 API。SQLDatabaseToolkit 连接数据库，让 Agent 能查表、跑 SQL、获取 schema 信息。VectorStoreToolkit 让 Agent 能查询向量数据库做 RAG 检索。
实战中经常需要自定义 Tool 和 Toolkit——把业务内部的 API 接口封装成 Tool，Agent 就能在你的业务系统中发挥作用。

---

**Q47：Agent 中的 Memory（记忆）有什么作用？怎么实现？**

Agent 需要 Memory 来维护对话上下文。没有 Memory，每次对话都是全新的，Agent 不知道前面说过什么、调过什么工具。
LangChain 的 Memory 分为两种：短期记忆——当前会话的对话历史，存在内存中，会话结束即消失。核心实现是 `ConversationBufferMemory`，简单存储所有对话消息，但随着对话增长 Token 消耗越来越大（每轮都要把所有历史发给 LLM）。`ConversationBufferWindowMemory` 是其优化版——只保留最近的 K 轮对话，防止 Token 爆炸。`ConversationSummaryMemory` 则用 LLM 对历史对话做摘要，用摘要代替原始对话节省 Token，但摘要本身也有精度损失。
长期记忆——跨会话持久存在的记忆，使用外部存储（数据库、Redis、向量库），用户下次来时还能记得之前的偏好和信息。实现方式通常和 RAG 结合——把历史对话的摘要或关键信息向量化存入向量库，新对话时检索相关历史记忆。
LangChain 中用 `create_tool_calling_agent` 创建 Agent 时传入 `chat_history` 参数来注入历史消息。也可以通过把 Memory 包装为 Agent 的 Tool 来实现更灵活的记忆管理。

---

**Q47-附：Multi-Agent 系统是什么？什么场景需要多 Agent？**

有些复杂任务单个 Agent 难以胜任——要么工具太多 LLM 选择困难，要么任务需要不同专业能力。多 Agent 系统把复杂任务拆给多个各司其职的 Agent 协作完成。
核心模式：每个 Agent 有独立的角色、工具集和 Prompt 设定。比如代码生成场景：Architect Agent（需求分析 + 架构设计）→ Coder Agent（编码实现）→ Reviewer Agent（代码审查）→ Tester Agent（测试验证），流水线式传递任务。
LangChain 没有内置成熟的多 Agent 框架（LangGraph 是为此而生的）。但可以用 LCEL 实现简单的多 Agent 编排——把不同 Agent 作为 Pipeline 的不同阶段串联起来，或者人工编写路由逻辑根据输入类型分发给不同的 Agent。
需要注意的是：多 Agent 系统极度消耗 Token（每个 Agent 都有自己的对话历史和 LLM 调用），在决定是否引入多 Agent 前，先评估单个 Agent 配合更多 Tools 是否已经够用。

---

## 背诵口诀

- **HTTP 三要素**：方法（GET/POST）+ URL（资源路径）+ 状态码（200/404/500）
- **TCP vs UDP**：TCP 可靠慢（握手+确认+重传），UDP 快不可靠（只管发不管到）
- **ACID 四字诀**：原子（全做或全不做）、一致（约束不违反）、隔离（并发不干扰）、持久（存了就丢不了）
- **进程线程协程**：进程独享内存，线程共享内存，协程用户态切换最快
- **同步异步扭在一起看**：同步 = 等人回话，异步 = 发完不管，阻塞 = 干等着，非阻塞 = 立刻知结果
- **RESTful 设计**：URL 只用名词，方法表达动作，版本号前缀，无状态传 Token
- **JSON vs XML**：JSON 体积小、解析快、JavaScript 原生支持，API 事实标准
- **加密两兄弟**：对称一把钥匙（快），非对称两把钥匙（安全），HTTPS 混合使用
- **ES 倒排索引**：文档分词 → 词建索引 → 词查文档 → 不用全表扫描
- **ES text vs keyword**：text 分词能搜，keyword 完整能排
- **ES 写入到可搜**：内存 Buffer → Refresh（1s）→ 可搜索 → Flush（30min）→ 持久化
- **ES Bool 查询**：must 算分 AND，filter 不算分 AND（快），should 算分 OR，must_not 不算分 NOT
- **Prompt 模板**：变量抽离，模板复用；System 定角色，Human 提问题
- **Runnable = LCEL 基石**：一切组件实现 Runnable 接口（invoke/ainvoke/stream/batch），管道符 `|` 是语法糖，本质构建 RunnableSequence。LangGraph 节点仍是 Runnable，但编排从管道升级为有向图
- **Few-Shot + CoT**：给例子学格式，写推理一步步来
- **RAG 全流程**：加载 → 分割 → 向量化 → 存库 → 问题向量化 → 检索 → 拼接 → 生成
- **RAG 精髓**：大模型有知识截止和幻觉问题，外挂知识库检索后生成，随时更新不重训
- **Chunk 分割**：大小看场景（512 FAQ / 1024 长文），重叠防切断（10-20%）
- **Chain 四兄弟**：Stuff 全塞一起（简单但怕长），Refine 逐篇改进（慢但精），Map-Reduce 先各自总结再汇总（快但丢细节），Map-Rerank 打分挑最佳（适合单篇就能答）
- **Agent vs Chain**：Chain 是固定流水线，Agent 是 LLM 自主决定步骤和工具调用
- **ReAct 循环**：Thought（想）→ Action（干）→ Observation（看结果）→ 循环直到能答
- **Tool 定义**：函数 + name + description（描述写得好，Agent 才用得对）
- **Agent Memory**：短期内存 Buffer/Summary，长期外存向量库
