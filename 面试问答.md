# 面试问答

## 1. 简单介绍一下你的项目

这个项目叫 Enterprise Multi-Agent RAG Assistant，本质上是一个企业级的智能知识库助手。你可以把它理解成一个能读懂企业内部文档、然后基于文档内容回答员工问题的 AI 助手。

举个例子，HR 部门上传了一份《员工制度管理手册》，员工就可以直接问"试用期多久""年假怎么算"，系统会自动从文档里检索出相关段落，然后用大模型生成一个流畅的自然语言回答，附上文档来源。

当然不止检索知识库，它还集成了图片生成、数学计算、时间查询、地图搜索这些能力。比如员工问"帮我生成一张请假流程图"，系统就会调 DashScope 的文生图接口画一张出来。问"2+3 等于多少"，就会调 MCP 计算器。

技术上用了 LangGraph 做 Agent 框架，FastAPI 做 Web 后端，Vue3 写前端，底层存储是 MySQL + Redis + Elasticsearch + ChromaDB 四件套。整个项目用 Docker Compose 一键部署，六个容器（MySQL、Redis、ChromaDB、ES、Backend、Frontend）一条命令全起来。

## 2. 为什么重排序采用 BGE 模型？

这个要从检索流程说起。用户问题进来后，会先走两路并行召回：一路是 ChromaDB 向量相似度检索，一路是 Elasticsearch 的 BM25 关键词匹配。两路各自返回 Top-15，然后做 RRF 融合得到一个 Top-6 的候选列表。

但这时候排序还不够精确。向量检索偏向语义相关，BM25 偏向关键词命中，两者都只是"看起来相关"，没有真正理解问题和文档之间到底匹不匹配。所以需要再加一层精排。

BGE（BAAI General Embedding）是智源研究院出的中文模型，选它的 rangeranker-base 版本有三个考虑。第一个，中文效果好，它是专门针对中文场景训练的，在中文的 MTEB 基准上排名靠前。第二个，架构优势，它是一个 CrossEncoder，不是 BiEncoder。BiEncoder 把问题和文档分别编码成两个向量再算余弦相似度，两个向量在编码阶段没有交互。CrossEncoder 是把问题和文档拼成一段文本，一起送进 Transformer，这样模型能看到"问题里的这个关键词"和"文档里的那句话"之间的精确对应关系。代价是计算量大，不适合海量文档召回，但几百条候选文档做精排是完全没问题的。第三个，实际效果说话——我们测过，重排序前 Top-1 准确率大概 70%，重排序后能到 90% 以上。

代码实现上用了一个懒加载的单例模式，毕竟模型文件有两百多兆，如果应用启动时就加载会很慢。所以第一次真正用到检索的时候才去加载，加载完就一直复用。

## 3. 记忆部分如何做的？

这个问题可以拆成三个层次来回答。

第一层是短期对话记忆。LangGraph 框架自带了一个叫 Checkpointer 的机制，它的作用是把 Agent 每轮对话的状态（主要就是 messages 列表）持久化存起来。我用的是 AsyncRedisSaver，底层走 Redis。这个 Checkpointer 按 thread_id 区分不同会话——我把 thread_id 直接设成 session_id，这样一个用户的一个会话，所有历史消息都能自动记住。用户在同一会话里第二轮发消息时，Agent 就已经能看到上一轮他说了什么、AI 回了什么。如果 Redis 挂了，会降级到 MemorySaver，也就是纯内存存储、重启就丢。

第二层是长期跨会话记忆。这一层的需求场景是：用户今天说"我叫小明，是技术部的"，过了一周再来，系统还能记住他叫小明。实现上用了 LangGraph 的 Store 机制，底层是 Redis Store。我封装了三个工具：remember 写入一个 key-value，recall 按 key 查询，forget 删除。Agent 的 System Prompt 里写好了规则，当用户分享个人信息时就调 remember，对话开始时调 recall 加载之前的记忆。这个 Store 是按 user_id 做了命名空间隔离的，不同用户的记忆不会串。

第三层是长对话摘要。如果一个人在一个会话里聊了几十轮，消息列表会越来越长，token 消耗也越来越多。我用 LangGraph 的 SummarizationMiddleware 来解决这个问题：当消息超过 30 条时自动触发摘要，把最早的 10 条压缩成一段摘要文本，保留最近的 20 条原文。这样既不会丢上下文，也不会撑爆 token 限制。

## 4. Agent 意图识别怎么做的？

说实话，这个问题我们踩过坑。

最开始的设计是走多 Agent 路由模式，用了一个叫 langgraph-supervisor 的库。一个 Supervisor Agent 分析用户意图，然后决定把请求转给 RAG Agent 还是 General Agent。这个方案看起来很美——每个 Agent 各司其职。但实际用下来问题特别多：Supervisor 经常来回路由停不下来，递归限制成了家常便饭；Supervisor 自己的路由指令还经常泄露到输出里，用户看到"FINISH"这种奇怪的字眼。

后来我彻底换了个思路——不要 Supervisor，不要多 Agent，就用一个 Agent，把所有工具都注册给它。意图识别这件事，本质上 LLM 自己就能做。你用 LangChain 的 create_agent 注册了一堆工具，每个工具有一个 name 和一个 description。Agent 看到用户问题后，自己判断应该调哪个工具。用户问"请假制度是什么"——哦，这需要查文档，调 search_knowledge_base。用户问"画一张星空图"——哦，这是要生成图片，调 generate_image。用户问"2+3 等于多少"——这个太简单了，调 calculator。

所以所谓的"意图识别"并不是一个独立的分类模块，它就是 LLM 在 ReAct 循环里做 Think 那一步的自然产物。这种方式的优势在于零额外开销——不需要训练分类器，不需要维护路由规则，LLM 的能力本身就是意图识别器。

## 5. Agent 流程 & 用户输入问题进来后流程是什么？

以"员工试用期多久"这个问题为例，一步一步走。

用户输入问题，点发送。前端先把消息显示在聊天框里，然后发一个 POST 请求到 /api/chat/stream。后端收到请求后，JWT 鉴权通过，先处理会话——新会话就在 MySQL 建一条记录，已有会话就查出来。然后保存用户消息。接着创建 Agent，挂上 Redis Checkpointer 加载历史对话，挂上所有工具。

Agent 开始跑了。第一步 Think，LLM 看到问题"试用期多久"，心想这是要查文档，决定调 search_knowledge_base 工具。第二步 Act，工具被调用，触发 RAG 检索管线：先把问题向量化，同时发到 ChromaDB 做语义检索和 Elasticsearch 做关键词检索，两路结果用 RRF 算法融合排序，取前六条送给 BGE CrossEncoder 精排，最后输出 Top-3 最相关的文档片段。第三步 Observe，Agent 拿到检索结果"试用期原则上为三个月，特殊岗位按劳动合同约定执行"。第四步再 Think，信息够了，不需要再调其他工具了，可以生成最终回答。最后一步，LLM 逐 token 生成："根据《员工制度管理手册》，试用期为三个月，特殊岗位按合同约定执行。📎 来源：《员工制度管理手册》"。

这些 token 不是一次性返回的，而是每生成一个 token 就被 astream_events 的 on_chat_model_stream 事件捕获，包装成 SSE 事件推给前端。前端收到一个 token 就往消息框里追加一个字，用户看到的就是字一个个蹦出来的效果。当 Agent 生成完最后一个字，后端发一个 StreamDone 事件，前端收到后关掉 loading 状态。

最后，完整的回答写入 MySQL，LangGraph Checkpointer 自动把本轮对话追加到 Redis。

## 6. MCP 是什么？

MCP 全称 Model Context Protocol，是 Anthropic 提出来的一套标准化协议，专门用来解决大模型和外部工具之间的通信问题。

可以这么理解：以前我们想让大模型调用外部工具，要么自己写适配代码把工具包装成 LangChain Tool，要么用 Function Calling 的格式自己去定义 schema。每接入一个新工具就要写一堆胶水代码。MCP 就是把这些胶水代码标准化了——它定义了一套 Client-Server 协议，工具提供方只需要按照 MCP 的规范写一个 Server，大模型这边通过 MCP Client 就能自动发现和调用这些工具。

我项目里集成了两个 MCP Server。一个是自己写的计算器 Server，跑在本机 8765 端口，用 FastMCP 框架几行代码就写好了。这个计算器做了一层安全防护——正则白名单只允许数字和数学运算符，然后用受限的 eval 执行计算，math 模块的函数比如 sqrt、pow 也都支持。另一个是高德地图的 MCP Server，这是阿里云 DashScope 平台托管好的，提供 16 个地图相关的工具，包括路线规划、周边搜索、天气查询这些。

MCP Client 这边我用了 langchain-mcp-adapters 这个库，配置一下 Server 地址和传输协议，它就会自动连接、发现工具、注册成 LangChain Tool。整个过程是异步的，应用启动时跑一次就行。

## 7. 会话流程是什么？

整个会话从创建到结束的背景逻辑是这样的。

用户打开前端页面时，左侧边栏会请求 GET /api/chat/sessions，把当前用户所有历史会话拉出来，按创建时间倒序展示。每个会话显示 title（取第一条用户消息的前 50 字）和日期。

用户输入问题点发送。前端先在本地维护的 messages 数组里加一条 user 消息，再加一条空的 assistant 消息作为占位——后面流式 token 就是往这个占位消息的 content 字段后面拼。然后发 POST /api/chat/stream，body 里带上 knowledge_base_id、question、可选的 session_id 和 model。

后端收到请求，JWT 鉴权通过后，第一步就是会话管理。如果前端没传 session_id，说明这是新会话，就在 MySQL 的 chat_sessions 表插一条记录，user_id 关联当前用户，title 取问题前 50 个字符，knowledge_base_id 存当前选中的知识库。如果前端传了 session_id，说明是已有会话继续聊，就去 MySQL 查这个 session 是否存在、是否属于当前用户，两个条件都满足才放行。然后把用户的问题存到 chat_messages 表，role 标为 user。

然后创建 Agent。这里有一个关键操作：把 LangGraph Checkpointer 的 thread_id 设成 session_id。这样如果这个 session 之前有对话历史，Agent 启动时会自动从 Redis 加载之前的全部消息作为上下文。如果是新会话，没有历史消息也不影响。

Agent 跑起来后，进入 ReAct 循环，每产出一个 token 就通过 SSE 推给前端。前端收到 data.session_id 事件就把 session_id 存下来，同时异步刷新侧边栏的会话列表（新会话会出现在列表顶部）。收到 data.token 就往当前 AI 消息的 content 上追加。收到 data.url 就把图片 URL 存到消息的 images 数组，渲染成带下载按钮的图片卡片。收到 data.done 就关掉 loading 状态。

流式结束后，后端的完整回答写入 chat_messages，role 标 assistant。LangGraph Checkpointer 自动把本轮消息列表追加到 Redis。

用户在侧边栏点击某个历史会话，前端请求 GET /api/chat/history/{session_id}，后端从 MySQL 查出这个 session 下面所有消息，按时间升序排列返回，前端一次性渲染全部对话。用户删除会话，后端删 MySQL 记录的同时，调用 checkpointer.delete_thread 把 Redis 里对应的对话状态也清掉。

总结一下：**MySQL 负责持久化、可查询的消息记录；Redis Checkpointer 负责 LangGraph 运行时对话状态的自动存取。两者用 session_id 串起来，同一个会话内的所有轮次自动关联**。

## 8. 流式输出怎么做的？为什么用 SSE 而不是 WebSocket？

流式输出的实现分前后端两段。后端用 LangGraph 的 astream_events，传 version="v2"，这是一个异步事件流接口。Agent 每生成一个 token，就会触发一个 on_chat_model_stream 事件，event 的 data.chunk.content 就是这一个 token 的文本。我把这个 token 包装成 TokenGenerated 的 SSE 事件——格式就是 `data: {"token": "试用期"}\n\n`——然后 yield 出去。

这里还有一个细节，图片生成的 URL 不是通过 token 流出来的，而是单独处理的。generate_image 工具执行完后，我把图片 URL 存到一个线程安全的模块级变量里，在 on_tool_end 事件里取出来，通过独立的 ImageGenerated 事件推给前端。这样就不会出现图片 URL 被当成文本流式输出的情况。

前端用原生 fetch 发请求，拿到 response.body 的 ReadableStream，然后循环 read 直到 done。每读到一个 SSE 事件，JSON.parse 后根据 key 分发处理——token 就拼到消息内容后面，url 就渲染成图片卡片，done 就关 loading。

为什么用 SSE 不用 WebSocket，就一个字：够用。LLM 生成回复这件事本身就是单向的——用户发一个问题，服务端流式返回一个回答。不存在服务端需要主动给客户端 Push 消息的场景。SSE 是基于 HTTP/1.1 的，浏览器原生支持，断了自动重连，Nginx 只需要关掉 proxy_buffering 就能透传。WebSocket 是双向通道，你要维护连接状态、处理心跳 Ping/Pong、断线要手动重连，还要考虑代理服务器的 WebSocket 升级问题。一句话，如果业务需求是单向的服务端推送，SSE 是最简单、最可靠的方案，WebSocket 属于过度设计。
